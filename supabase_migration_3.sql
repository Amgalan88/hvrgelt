-- ============================================================
--  hvrgelt — Migration 3: "Карго авах" нэг товчны чиглэл
--  Supabase Dashboard → SQL Editor → энэ бүхнийг хуулж RUN дарна.
-- ============================================================

-- Захиалагчийн хадгалсан карго чиглэл (авах цэг + хүргэх хаяг)
alter table customers
  add column if not exists cargo_route jsonb;
