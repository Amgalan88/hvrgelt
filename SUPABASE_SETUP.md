# hvrgelt — Supabase тохиргооны заавар

Энэ апп одоо Supabase (өгөгдлийн сан) дээр ажиллахаар бэлэн боллоо.
Доорх 4 алхмыг дагаснаар бүх захиалга, бүртгэл бодитоор хадгалагдаж,
жолооч/оператор хооронд real-time хуваалцагдана.

---

## 1. Supabase project үүсгэх

1. https://supabase.com → **Sign in** (GitHub/Google-ээр үнэгүй).
2. **New project** дарна.
   - Name: `hvrgelt`
   - Database Password: хүчтэй нууц үг (хадгалж аваарай)
   - Region: `Southeast Asia (Singapore)` (Монголд хамгийн ойр)
3. Project үүсэхийг ~2 минут хүлээнэ.

## 2. Хүснэгтүүдийг үүсгэх (SQL)

1. Зүүн талын цэснээс **SQL Editor** → **New query**.
2. Төслийн дотор байгаа [supabase_schema.sql](supabase_schema.sql) файлын
   **бүх агуулгыг** хуулж, query цонхонд буулгана.
3. **Run** (эсвэл Ctrl+Enter) дарна.
4. "Success" гарвал хүснэгт + анхны өгөгдөл орсон гэсэн үг.

## 3. API түлхүүрээ авах

1. Зүүн доод талын **Project Settings** (араат дугуй) → **API**.
2. Дараах 2 утгыг хуулж ав:
   - **Project URL** (жишээ: `https://abcdxyz.supabase.co`)
   - **anon public** key (`eyJ...` гэж эхэлдэг урт түлхүүр)

## 4. .env файл үүсгэх

1. Төслийн үндсэн фолдерт `.env.example`-г хуулж `.env` нэртэй болго.
2. Дотор нь дээр авсан 2 утгаа бөглө:

```
VITE_SUPABASE_URL=https://abcdxyz.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi... (anon public key)
```

3. Хадгална. (`.env` нь git-д орохгүй — нууц хэвээр үлдэнэ.)

---

## Ажиллуулах

```
npm run dev
```

Браузерт нээгдсэн линкээр орж туршина.

### Туршилтын нэвтрэх мэдээлэл (seed дата)

| Үүрэг       | Утас      | Нэвтрэх          |
|-------------|-----------|------------------|
| Супер админ | 99739959  | нууц үг: gegee0011 |
| Оператор    | 99110001  | PIN: 1234        |
| Жолооч      | 99112233  | PIN: 1111        |
| Захиалагч   | шинээр бүртгүүлнэ | —          |

### Шалгах зүйл
- Захиалга үүсгээд хуудсаа **refresh** хий → захиалга **арилахгүй** байх ёстой.
- Хоёр өөр браузер (эсвэл утас + комьпютер) дээр оператор + жолоочоор нэвтэрвэл
  нэг нь захиалга томилоход нөгөөд нь **шууд (real-time)** гарч ирнэ.

---

## Migration — нэг л удаа ажиллуулна

`supabase_schema.sql`-ыг ажиллуулсны дараа **`supabase_migration_all.sql`**
файлыг Supabase Dashboard → SQL Editor → New query дээр бүхэлд нь хуулж
тавиад RUN дарна. Энэ нэг файл 11–17 дугаарын бүх өөрчлөлтийг агуулна:

| № | Юу нэмдэг вэ |
|---|---|
| 11 | Захиалгын үйлчилгээний төрөл (`service_id`) |
| 12 | Жолоочийн баримт бичиг (зураг, үнэмлэх, машин, дугаар, баталгаажуулалт) |
| 13 | Дэд төрөл + ачааны мэдээлэл (зураг, кг, хагарах, яаралтай) |
| 14 | Онлайн төлбөр (`payments` хүснэгт, захиалгын төлбөрийн төлөв) |
| 15 | Хүргэлтийн дараах 1–5 оддын үнэлгээ |
| 16 | Санал хүсэлт |
| 17 | Партнёрын бараа, сагс, газрын төлбөрийн QR |

Файлын төгсгөлд шалгах query байгаа — үр дүнгийн бүх мөр дээр ✅ гарвал
бүгд амжилттай. Дахин ажиллуулахад аюулгүй (байгаа зүйлээ давхардуулахгүй).

`supabase_migration_11.sql` … `_17.sql` гэсэн тусдаа файлууд түүхийн
төлөө үлдсэн — тэдгээрийг дахин ажиллуулах шаардлагагүй.

`supabase_migration_18.sql` нь **утасны дугаарын нууцлалыг** техникийн
түвшинд хатууруулах заавар. Одоогоор бүх мөр нь тайлбар болгож хаагдсан
байгаа — Supabase Auth руу шилжсэний дараа идэвхжүүлнэ (одоо ажиллуулбал
апп өгөгдлөө уншиж чадахгүй болно).

Апп нь эдгээр багана байхгүй үед ч унахгүй ажиллахаар бичигдсэн — гэхдээ
шинэ боломжууд (үйлчилгээний сонголт, төлбөр, үнэлгээ, партнёрын бараа)
ажиллахгүй.

---

## Онлайн төлбөр холбох (QPay / Bonum)

Төлбөрийн түлхүүр **client талд орохгүй** — Edge Function дотор л байна.

```bash
supabase functions deploy create-payment --no-verify-jwt
supabase functions deploy payment-webhook --no-verify-jwt

# QPay
supabase secrets set PAYMENT_PROVIDER=qpay   QPAY_USERNAME=... QPAY_PASSWORD=... QPAY_INVOICE_CODE=...   PUBLIC_CALLBACK_URL=https://<project>.functions.supabase.co/payment-webhook
```

Дараа нь `.env`-д `VITE_PAYMENT_PROVIDER=qpay` гэж бичнэ.

**Bonum** ашиглах бол merchant эрхээ авсны дараа
`supabase/functions/create-payment/index.ts` доторх `createBonumInvoice()`-ыг
өөрийн доктой тулгаж эндпойнт/талбарын нэрийг тааруулна.

Хоосон орхивол апп **гарын авлагын горимд** ажиллана: үйлчлүүлэгчид
дансны мэдээлэл харуулж, оператор төлбөрийг гараар баталгаажуулна.

---

## Санал хүсэлтийн и-мэйл

```bash
supabase functions deploy send-feedback-email --no-verify-jwt
supabase secrets set FEEDBACK_EMAIL_TO=<хүлээн авах и-мэйл> RESEND_API_KEY=...
```

Тохируулаагүй ч санал `feedback` хүснэгтэд хадгалагдаж, супер админы
"Санал" таб дээр харагдана.

---

## ⚠️ Аюулгүй байдлын анхааруулга (чухал)

Одоогийн тохиргоо нь **демо/тест** зориулалттай:

- PIN/нууц үг өгөгдлийн санд **задгай текстээр** хадгалагдаж байгаа.
- RLS policy нь хэн ч (anon) бүх өгөгдлийг унших/бичих эрхтэй.

**Бодит хэрэглээнд гаргахаасаа өмнө** дараахыг хийх шаардлагатай:
1. Supabase Auth (жинхэнэ нэвтрэлт) рүү шилжих.
2. Нууц үгийг hash хийх.
3. RLS policy-г үүрэг тус бүрээр хязгаарлах (зөвхөн өөрийн өгөгдлийг харах гэх мэт).

Эдгээрийг хийх бол надад хэлээрэй.

---

## Дараагийн алхам: интернэтэд байршуулах (deploy)

`.env` ажиллаж, локалд тест хийсний дараа Vercel/Netlify дээр гаргаж
бусдад линкээр үзүүлж болно. Бэлэн болсон үед хэлээрэй — заавал тусална.
